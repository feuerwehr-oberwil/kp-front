# Ingest host secrets. Copy to .env — which is gitignored, and must stay that way.
# NOTE: none of these are the client credential. That one is public on purpose and lives in
# backend/app/telemetry/dsn.py; see PRIVACY.md for why that is not a mistake.

# openssl rand -hex 32
SECRET_KEY=
# openssl rand -hex 24
POSTGRES_PASSWORD=

GLITCHTIP_DOMAIN=https://ingest.kp-front.ch
DEFAULT_FROM_EMAIL=ingest@kp-front.ch
